# 디자인 원칙 — Evidence-first + Chat-first

배포 전 `product.md`, `analytics.md`, `deploy.md`와 함께 읽는다.

## 정보 구조

1. Hero: `Portfolio` · `AI Product Manager` · `7 years of experience` 독립 라벨, 고객 문제 해결 서사, 핵심 경험 태그·확인 수치·협업사 로고. Hero 내부 CTA는 두지 않는다.
2. About: `저는 제너럴리스트이자 AI 스페셜리스트입니다.` 아래 사용자 제공 인물 이미지와 문제 정의 → 우선순위 → 결과 검증의 제품 접근 방식
3. Projects: `기여도가 높은 프로젝트를 소개합니다.` 아래 문제 → 판단·실행 → 성과 → 본인 책임 → 공개 가능한 시각 자료
4. Experience: Adler를 제외한 최신순 타임라인 6개, 기간별 재직 개월 배지와 선호 조직 방향
5. Contact: 다른 섹션과 동일한 왼쪽 축에 `Contact` eyebrow와 `채용·협업` 제목, 모바일·태블릿에서는 읽기 폭 `max-w-3xl`, 데스크톱에서는 섹션 컨테이너 폭을 사용하는 문의 폼
6. 챗봇: 화면 위의 보조 탐색 레이어

## 톤앤매너

- 기본은 검정 표면과 흰색 정보, 코럴 `#ff6645`를 결정·행동 포인트에만 사용한다.
- `colorflow-animation.mp4`는 전체 포트폴리오의 낮은 대비 ambient 배경이다. 한 번만 재생하고 reduced motion·데이터 절약 모드에서는 정적 그라데이션으로 대체한다.
- `background.png`는 Hero 장식 배경이다. 텍스트 가독성을 위한 방향성 검정 scrim은 유지하되 레이어 opacity를 `0.88`로 제한해 활동 맥락이 식별되게 하며, 경력 증거 이미지로 오해시키지 않는다.
- 카드와 본문은 사진·영상보다 우선한다. 배경 자산 실패 시에도 동일한 정보 위계가 남아야 한다.
- 폰트는 기존 Pretendard를 재사용하고 새 웹폰트를 추가하지 않는다.

## 프로젝트 탐색

- 독립 `Evidence Product` 분류·아코디언은 사용하지 않는다. About 다음에 프로젝트 카드 5개를 직접 노출한다.
- 모든 프로젝트 이름·문제·판단·실행·성과·본인 책임은 `src/data/portfolio.ts`의 확인된 사실만 사용한다.
- 이미지가 아직 없어도 프로젝트 이름과 근거 텍스트는 DOM에서 읽을 수 있어야 한다.
- Navbar 순서와 화면 순서를 `About → Projects → Experience → Contact`로 일치시키고 scroll margin을 보장한다.

## Hero

- Navbar는 `Kyoungmin Lee`를 영문 브랜드로 표시한다. 이름·포트폴리오 정체성은 Navbar가 담당해 Hero 안에서 반복하지 않는다.
- Hero 상단은 `Portfolio`, `AI Product Manager`, `7 years of experience`를 각각 독립 배지로 표시한다. 모바일에서는 배지 내부가 아니라 배지 사이에서만 줄바꿈한다.
- 헤드라인은 `고객의 문제를 제품으로 해결합니다.` 한 문장으로 구성하고, `고객`과 `제품`을 코럴로 강조한다.
- Hero 설명 문단은 노출하지 않고 헤드라인 다음에 경험 태그를 바로 배치한다.
- 경험 태그는 `프로덕트의 제로투원 경험`, `350만 MAU 제품 운영`, `B2B&B2G 프로젝트`, `B2C Product 기획·개발`을 사용한다.
- 지표는 `5+ / 수행 프로젝트`, `3+ / 제품 기획.운영`, `28억 / 매출 기여`를 표시한다. 세 값은 사용자 직접 확인값이며 별도 프로젝트·기간·매출 유형으로 임의 확장하지 않는다.
- 지표의 숫자는 새로고침 뒤 `0`에서 최종값까지 약 1.6초 동안 한 번만 증가한다. 숫자 폭은 tabular number로 안정화하고, 스크린리더에는 최종값만 제공한다.
- 지표 아래에는 별도 `Project partner company` 제목 없이 NIPA → 부산광역시 → LG유플러스 → KISA → 네오사피엔스 → NHN Cloud → Syrup Wallet → Fixness 순서의 투명 단색 로고를 표시한다. 모바일·태블릿은 4×2, 데스크톱은 8×1 compact grid를 사용하고 회사별 맞춤 Hero에는 노출하지 않는다.
- Hero는 콘텐츠가 잘리지 않는 범위에서 `min(100svh, 52rem)` 높이를 사용해 세로가 긴 화면에서도 다음 About 영역이 이어져 보이게 한다.
- Hero에는 프로젝트 버튼과 챗봇 버튼을 모두 두지 않는다. 챗봇은 Navbar의 얼굴 이미지가 있는 `이경민 AI` 하나로만 연다.

## 이미지·차트 영역

- About 인물 이미지는 왼쪽 제목 아래 독립 요소로 배치한다. 모바일은 176px, `sm`은 208px, 데스크톱은 약 272px이며 모바일 중앙·데스크톱 왼쪽 정렬을 사용한다.
- About 인물 원본은 548×548px 투명 PNG다. 무채색·중립 배경 위에 `object-cover object-center`, 명시적 width/height, `loading="lazy"`, `decoding="async"`로 정사각형 영역을 안정적으로 채운다. 본문 배경이나 경력 증거 이미지로 사용하지 않는다.
- 프로젝트는 모든 화면 폭에서 한 행에 하나씩 배치한다. 증거 이미지 영역은 프로젝트 카드 폭 전체를 사용하고 4:3 고정 프레임 안에서 `object-contain`으로 원본을 자르지 않는다.
- 모든 증거 이미지는 `evidence-images.md`에 따라 권장 4:3, 1600×1200px PNG로 제작해 `public/evidence/<project-id>/` 또는 공개 Supabase Storage URL에 등록한다.
- 초기에는 `증거 이미지 준비 중`만 표시하고 필요한 공개 자료의 3줄 목록은 노출하지 않는다.
- PNG가 제공되면 같은 데이터 구조에서 2초마다 오른쪽에서 왼쪽으로 천천히 이동하는 슬라이드로 표시한다.
- 이전·다음 버튼과 점 내비게이션은 한 줄로 제공하고, 일시정지·재생은 이미지 위에 배치한다. 이미지 아래에 별도 캡션·상태 행을 두지 않는다.
- 마우스 hover, 키보드 focus, 확대 dialog, 화면 밖, 숨겨진 탭에서는 자동 이동을 멈추고 사용자가 조작할 수 있는 일시정지·재생 버튼을 제공한다.
- `prefers-reduced-motion: reduce`에서는 자동 이동과 전환 애니메이션을 끈다. 자동 이동은 live region으로 반복 낭독하지 않고 사용자가 직접 이동한 결과만 알린다.
- 이미지를 선택하면 확대 dialog를 열고 Escape·닫기·포커스 복귀를 지원한다.
- 모든 이미지에 사실을 설명하는 alt를 둔다. caption은 선택값이며 등록하더라도 화면 아래 문구로 노출하지 않고 확대 dialog의 보조기술 설명으로만 사용한다.
- 이미지는 lazy load, 고정 aspect ratio와 명시적 크기로 레이아웃 이동을 막는다.

## 챗봇

- Navbar의 얼굴 이미지가 있는 `이경민 AI`를 유일한 챗봇 진입점으로 사용한다. Hero와 우하단에는 중복 런처를 두지 않는다.
- 상단 CTA를 누르면 별도 pre-chat 팝업이 아니라 실제 Typebot 채팅창을 연다.
- 첫 안내, 추천 질문, 직접 입력, 개인정보 안내는 채팅창 경계 안에 표시한다.
- 추천 질문은 확인된 역량·프로젝트와 1:1로 연결한다.
- 첫 질문 뒤에도 같은 코럴 헤더와 Pretendard, 흰색 surface를 유지한다. 헤더는 얼굴 이미지와 `이경민 AI` 한 줄만 남기고 첫 안내 메시지에도 같은 얼굴 이미지를 사용해 대화 기록의 세로 공간과 정체성을 확보한다.
- 채팅창은 최대 `480×820px`이되 작은 화면에서는 viewport 안에 맞춘다.
- 답변 이후에는 입력폼 위에 검증된 후속 질문 3개를 항상 표시한다. 질문은 44px 이상의 가로 슬라이드 rail로 제공하고, 입력은 채팅창 가용 폭을 전부 사용한다. Typebot 기본 축소 입력은 화면에서 숨긴다.
- 자유 질문과 답변은 URL, 분석, prefilled variable로 보내지 않는다.
- Typebot 입력은 공식 `setInputValue` → `submitInput` 경로를 사용한다.
- 챗봇이 실패해도 프로젝트 직접 보기와 기본 정적 탐색은 남는다.

## 반응형·접근성·성능

- 필수 검사 폭: 390×844, 768×900, 1440×900.
- 본문 최소 터치 영역 44px, 챗봇 입력 글자 16px, 가시 포커스를 유지한다.
- 가로 넘침, Hero 텍스트 잘림, fixed 런처의 CTA 가림을 허용하지 않는다.
- Hero H1은 첫 프레임부터 보이며 핵심 정보에 opacity 진입 애니메이션을 적용하지 않는다.
- About 전체 grid, Projects header·각 카드, Experience header·각 경력·커리어 방향, Contact header·form은 viewport 진입 시 `opacity 0 → 1`, `y 18px → 0`으로 한 번만 나타난다. 긴 section 전체를 opacity 0으로 감싸지 않는다.
- 전역 `MotionConfig reducedMotion="user"`와 각 진입 요소의 reduced-motion 분기로 모션 감소 환경에서는 변환·카운트업 없이 최종 상태를 즉시 표시한다.
- Typebot은 초기 preload하지 않고 사용자가 Navbar의 `이경민 AI`를 선택한 뒤에만 불러온다.
- 원격 배경은 장식 자산이며 실패·차단·느린 네트워크에서 CSS fallback을 사용한다.
- 관리자·404에는 포트폴리오 ambient, Typebot, 분석을 노출하지 않는다.
- Contact 다음에 별도 저작권 Footer를 추가하지 않는다.
